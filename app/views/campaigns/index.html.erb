
<% @campaigns.each do |campaign| %>
    <%= render campaign%>
<% end %>
<%= link_to t('.new', :default => t("helpers.links.new")),
    new_advertiser_campaign_path,
    :class => 'btn btn-primary' %>
